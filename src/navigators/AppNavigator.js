import {createstack} from "react-navigation";
import {View} from "react-native";
class AppNavigator extends Component {
    constructor(props) {
        super(props);
        this.state = {  };
    }
    render() {
        return (
            <View>Some content</View>
        );
    }
}

export default AppNavigator;